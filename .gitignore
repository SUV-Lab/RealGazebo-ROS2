build
install
log
__pycache__

# Large STL mesh files (downloaded by CMake, see WORLD_CTRACK).
# urban.stl belongs here too: it used to be the one tracked mesh, which meant
# a build whose checksum differed from the server's silently rewrote a tracked
# 40 MB file and left every working tree dirty.
c-track.stl
vils.stl
urban.stl

# Legacy artifact of the retired generate_compose flow
docker-compose.override.yml

# Auto-generated from Jinja2 templates at launch time
src/realgazebo/models/c-track/model.sdf
config/
